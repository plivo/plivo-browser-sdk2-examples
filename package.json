{
  "name": "plivo-sample-app",
  "description": "Plivo SDK example app",
  "version": "2.0.6-rc.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "beta-version-patch": "npm version $(semver $npm_package_version -i prepatch --preid beta)",
    "rc-version-patch": "npm version $(semver $npm_package_version -i prepatch --preid rc)"
  },
  "dependencies": {
    "ejs": "~1.0.0",
    "express": "^4.9.8"
  },
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-express": "^0.3.5"
  }
}
